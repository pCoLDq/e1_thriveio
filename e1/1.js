// Определите переменные str, num, flag и txt со значениями «Привет», 123, true, «true».
// При помощи оператора определения типа убедитесь, что переменных принадлежат типам: string, number, boolean.

let str = 'Привет';
let num = 123;
let flag = true;
let txt = 'true';

if (typeof str === 'string') {
  console.log("The type of 'str' variable is string");
}
if (typeof num === 'number') {
  console.log("The type of 'num' variable is number");
}
if (typeof flag === 'boolean') {
  console.log("The type of 'flag' variable is boolean");
}
if (typeof txt === 'string') {
  console.log("The type of 'txt' variable is string");
}
